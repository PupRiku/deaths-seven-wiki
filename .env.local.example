# Hash of the DM passphrase. Generate with:
#   node -e "console.log(require('crypto').createHash('sha256').update('your-passphrase').digest('hex'))"
# Then paste the hex output below.
DM_PASSPHRASE_HASH=
